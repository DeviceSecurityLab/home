# Device Security Lab — Home

osamusic.org のホームページ（静的サイト）。GitHub Pages で公開しています。

## 構成

- `index.html` — トップページ（自己完結。CSS/JS インライン）
- `assets/dsl-icon.png` — アイコン
- `CNAME` — カスタムドメイン `osamusic.org`
- `.nojekyll` — Jekyll 処理を無効化

## 公開

`main` ブランチのルートを GitHub Pages のソースに設定しています。`main` への push で自動的に反映されます。
